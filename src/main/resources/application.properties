spring.application.name=event.reservation

server.port=8081

# define driver db
spring.datasource.driver-class-name=org.postgresql.Driver
spring.datasource.username=postgres
spring.datasource.password=postgres
spring.datasource.url=jdbc:postgresql://localhost:5432/event_reservation

# define orm dialect
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# security
jwt.secret = SuperSecretAccessTokeni90sdfji323kofi0sdfi093f3rasko0s
jwt.appName = Tokonyadia App
jwt.jwtExpiration = 3600